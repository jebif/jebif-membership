#!/usr/bin/env python

from membership.models import *

infos = MembershipInfo.objects.filter(active=True, deleted=False, 
				inscription_date=datetime.date.today()-datetime.timedelta(1))

for info in infos :
	print ("%s %s <%s>" % (info.firstname, info.lastname, info.email))


